<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#1A1A1A"
    android:padding="8dp">

    <!-- Main content -->
    <LinearLayout
        android:id="@+id/widget_data_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:visibility="visible">

        <TextView
            android:id="@+id/widget_project_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Project"
            android:textColor="#CCCCCC"
            android:textSize="9sp"
            android:maxLines="1"
            android:ellipsize="end"
            android:gravity="center" />

        <!-- Numbers section -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center"
            android:layout_marginTop="2dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="24h"
                android:textColor="#AAAAAA"
                android:textSize="8sp" />

            <TextView
                android:id="@+id/widget_total_users"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="—"
                android:textColor="#FFFFFF"
                android:textSize="20sp"
                android:textStyle="bold"
                android:layout_marginStart="4dp" />

        </LinearLayout>

        <!-- Chart view -->
        <ImageView
            android:id="@+id/widget_chart"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:layout_marginTop="4dp"
            android:scaleType="fitXY"
            android:contentDescription="Users trend" />

        <TextView
            android:id="@+id/widget_last_updated"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="—"
            android:textColor="#999999"
            android:textSize="7sp"
            android:gravity="center"
            android:layout_marginTop="2dp" />

    </LinearLayout>

    <!-- No project selected -->
    <TextView
        android:id="@+id/widget_no_project"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:text="Tap to\nconfigure"
        android:textColor="#AAAAAA"
        android:textSize="10sp"
        android:gravity="center"
        android:visibility="gone" />

    <!-- Lock overlay -->
    <LinearLayout
        android:id="@+id/widget_lock_overlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#CC000000"
        android:orientation="vertical"
        android:gravity="center"
        android:visibility="gone">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="🔒"
            android:textSize="18sp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Pro"
            android:textColor="#FFFFFF"
            android:textSize="9sp"
            android:layout_marginTop="4dp" />

    </LinearLayout>

</FrameLayout>
