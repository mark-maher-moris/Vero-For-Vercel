name: vero
description: "Vero For Vercel"
publish_to: 'none'

version: 1.0.11+11

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.6.0
  shared_preferences: ^2.5.5
  google_fonts: ^8.0.2
  provider: ^6.1.5+1
  url_launcher: ^6.3.2
  share_plus: ^12.0.1
  timeago: ^3.7.1
  flutter_dotenv: ^5.2.1
  superwallkit_flutter: ^2.4.12
  webview_flutter: ^4.13.1
  in_app_review: ^2.0.10
  flutter_secure_storage: ^10.0.0
  flutter_globe_3d: ^2.2.5
  fl_chart: ^1.2.0
  home_widget: ^0.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
    - .env
